---
name: ljg
description: Route explicit `ljg` or named `ljg-*` requests; never trigger on generic analysis.
---

# LJg router

Choose exactly one sibling `ljg-*` method from the user's explicit name or intent. Read that sibling's `SKILL.md` directly and follow it as disclosed reference; do not invoke multiple methods unless the chosen workflow explicitly composes them.

Primary distinctions:

- concept anatomy → `ljg-learn`; vertical root cause → `ljg-think`; minimal A→B essence → `ljg-is`;
- domain generators → `ljg-rank`; defining constraints → `ljg-constraint`; mechanism in supplied information → `ljg-structure`;
- whole book → `ljg-book`; paper → `ljg-paper`; guided text reading → `ljg-read`; plain rewrite → `ljg-plain`;
- article writing → `ljg-writes`; Q&A extraction → `ljg-qa`; situational teaching → `ljg-teach`;
- relationship analysis → `ljg-relationship`; roundtable → `ljg-roundtable`; investment analysis → `ljg-invest`;
- blind-spot review → `ljg-blind`; content card → `ljg-card`; slide deck → `ljg-present`; English word → `ljg-word`.

If two methods remain plausible, recommend one and ask one bounded question. Never silently blend their contracts.
